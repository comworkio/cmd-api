stages:
  - mirror
  - publish

github-mirror:
  stage: mirror
  script:
    - setsid ./ci/github-mirror.sh
  only:
    - /^(master.*)$/
  tags:
    - cmd-api

cmd-api:
  stage: publish
  script:
    - setsid ./ci/docker-deliver.sh "cmd-api" "2.0"
  only:
    - /^(master.*)$/
  tags:
    - cmd-api
