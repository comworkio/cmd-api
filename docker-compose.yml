version: "3.3"

services: 
  cmd-api:
    image: comworkio/cmd-api
    user: root
    build:
      context: .
      dockerfile: Dockerfile
    environment: 
      - API_CMD=ls -l
    ports: 
      - 8080:8080
